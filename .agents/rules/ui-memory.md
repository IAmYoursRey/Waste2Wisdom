# Waste2Wisdom UI Memory

The UI designer owns the canonical design map.

Before changing UI:
- read .agents/context/ui-map.md
- inspect the current target files
- update the UI map if structure, layout, component placement, spacing, tokens, or interaction patterns change

The UI map must describe actual placement, not aspirations.

Never invent coordinates or components that do not exist.
